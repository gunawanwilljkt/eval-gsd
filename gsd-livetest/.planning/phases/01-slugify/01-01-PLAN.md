---
phase: 01-slugify
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/slugify.mjs]
autonomous: true
requirements: [REQ-01, REQ-02]
must_haves:
  truths:
    - "slugify('Hello World') returns 'hello-world'"
    - "slugify('  A__B--C!  ') returns 'a-b-c'"
  artifacts:
    - path: "src/slugify.mjs"
      provides: "slugify(text) ESM export"
      contains: "export function slugify"
  key_links: []
---

<objective>
Implement `slugify(text)` so both locked eval-contract rows go green.

Purpose: deliver REQ-01 and REQ-02.
Output: src/slugify.mjs exporting slugify.
</objective>

<execution_context>
@/Users/gunawanwilljkt/harness/eval-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunawanwilljkt/harness/eval-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-slugify/01-SPEC.md
@.planning/phases/01-slugify/01-EVAL-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement slugify(text)</name>
  <files>src/slugify.mjs</files>
  <read_first>.planning/phases/01-slugify/01-SPEC.md, .planning/phases/01-slugify/01-EVAL-CONTRACT.md</read_first>
  <eval_rows>EC-1, EC-2</eval_rows>
  <action>Create src/slugify.mjs exporting `export function slugify(text)`. Lowercase the input, replace every run of non-alphanumeric characters with a single `-`, then strip leading/trailing `-`. Implementation hint: `String(text).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '')`. No dependencies.</action>
  <verify>
    <automated>node --input-type=module -e "import {slugify} from './src/slugify.mjs';if(slugify('Hello World')!=='hello-world')process.exit(1);console.log('EC-1 PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - src/slugify.mjs contains `export function slugify`
    - `node --input-type=module -e "import {slugify} from './src/slugify.mjs';if(slugify('Hello World')!=='hello-world')process.exit(1);console.log('EC-1 PASS')"`
    - `node --input-type=module -e "import {slugify} from './src/slugify.mjs';if(slugify('  A__B--C!  ')!=='a-b-c')process.exit(1);console.log('EC-2 PASS')"`
  </acceptance_criteria>
  <done>Both eval-contract rows EC-1 and EC-2 exit 0 (green).</done>
</task>

</tasks>

<verification>
Both contract rows EC-1, EC-2 green; src/slugify.mjs exists with the slugify export.
</verification>

<success_criteria>
- slugify('Hello World') === 'hello-world'
- slugify('  A__B--C!  ') === 'a-b-c'
</success_criteria>

<output>
Create `.planning/phases/01-slugify/01-01-SUMMARY.md` when done.
</output>
