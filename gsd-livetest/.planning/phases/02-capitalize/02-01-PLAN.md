---
phase: 02-capitalize
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/capitalize.mjs]
autonomous: true
requirements: [REQ-03, REQ-04]
must_haves:
  truths:
    - "capitalize('hELLO') returns 'Hello'"
    - "capitalize('') returns ''"
  artifacts:
    - path: "src/capitalize.mjs"
      provides: "capitalize(text) ESM export"
      contains: "export function capitalize"
  key_links: []
---

<objective>
Implement `capitalize(text)` so both locked eval-contract Code rows (EC-3, EC-4) go green.

Purpose: deliver REQ-03 and REQ-04.
Output: src/capitalize.mjs exporting capitalize.
</objective>

<execution_context>
@/Users/gunawanwilljkt/harness/eval-gsd/.claude/get-shit-done/workflows/execute-plan.md
@/Users/gunawanwilljkt/harness/eval-gsd/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-capitalize/02-SPEC.md
@.planning/phases/02-capitalize/02-EVAL-CONTRACT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement capitalize(text)</name>
  <files>src/capitalize.mjs</files>
  <read_first>.planning/phases/02-capitalize/02-SPEC.md, .planning/phases/02-capitalize/02-EVAL-CONTRACT.md</read_first>
  <eval_rows>EC-3, EC-4</eval_rows>
  <action>Create src/capitalize.mjs exporting `export function capitalize(text)`. Coerce the input with `String(text)`, then upper-case the first character and lower-case the remainder. Empty-safe: return `''` for empty input (no crash). Implementation hint: `const s = String(text); return s.charAt(0).toUpperCase() + s.slice(1).toLowerCase();` — for empty input `charAt(0)` and `slice(1)` both yield `''`, so the result is `''`. ASCII single-token only; no Unicode-locale casing, no word-by-word title case (per 02-SPEC non-goals). Zero dependencies, matching Phase 1 style.</action>
  <verify>
    <automated>node -e "import('./src/capitalize.mjs').then(m=>process.exit(m.capitalize('hELLO')==='Hello'?0:1)).catch(()=>process.exit(1))"</automated>
  </verify>
  <acceptance_criteria>
    - src/capitalize.mjs contains `export function capitalize`
    - node -e "import('./src/capitalize.mjs').then(m=>process.exit(m.capitalize('hELLO')==='Hello'?0:1)).catch(()=>process.exit(1))"
    - node -e "import('./src/capitalize.mjs').then(m=>process.exit(m.capitalize('')===''?0:1)).catch(()=>process.exit(1))"
  </acceptance_criteria>
  <done>Both Code eval-contract rows EC-3 and EC-4 exit 0 (green). EC-5 is a Human row — not gated here; it is carried to verify-phase / 02-UAT.md.</done>
</task>

</tasks>

<verification>
Both Code contract rows EC-3, EC-4 green; src/capitalize.mjs exists with the capitalize export. EC-5 (Human) is verified at verify-phase via 02-UAT.md, not in this plan.
</verification>

<success_criteria>
- capitalize('hELLO') === 'Hello'
- capitalize('') === ''
</success_criteria>

<output>
Create `.planning/phases/02-capitalize/02-01-SUMMARY.md` when done.
</output>
