# 02-CONTEXT — capitalize (decisions)

Locked decisions for the phase (discuss-phase equivalent), so the planner has its context.

## Decisions
- ESM module at `src/capitalize.mjs`, zero dependencies (matches Phase 1 style).
- Single function `capitalize(text)`; first char upper, remainder lower; empty-safe.
- Eval-gated per the locked `02-EVAL-CONTRACT.md`.

## Gray areas
- None — trivial deterministic target.
